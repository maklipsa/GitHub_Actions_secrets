# This is a basic workflow to help you get started with Actions

name: passing job values

# Controls when the workflow will run
on:
  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:
  
jobs:
  producer:
    env:
      name: "Ptyś"
    runs-on: ubuntu-latest
    outputs:
      full_sentence: ${{ steps.producer.sentence_maker.SENTENCE }}
    steps:
      - name: Make a sentence
        id: sentence_maker
        run: |
          echo "SENTENCE='Ala ma kota $name'" >> $GITHUB_OUTPUT

  consumer:
    needs: [producer]
    runs-on: ubuntu-latest
    env:
      SENTENCE: ${{ needs.producer.outputs.full_sentence }}
    steps:
      - name: Print a sentence
        run: |
          echo "$SENTENCE"
